// const sum = require('./sum')
const _ = require('lodash')

const input = [1,2,3,4,5];

const calculator = () => {
    console.log(`Sum is ${sum.apply(null, input)}`);
}

module.exports = exports = calculator;

